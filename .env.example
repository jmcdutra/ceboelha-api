# =============================================================================
# Ceboelha API - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# =============================================================================
# Server
# =============================================================================
PORT=3333
NODE_ENV=development

# =============================================================================
# Database
# =============================================================================
# Local sem autenticação (desenvolvimento)
MONGODB_URI=mongodb://localhost:27017/ceboelha

# Local com autenticação (Docker/Produção)
# MONGODB_URI=mongodb://admin:password@localhost:27017/ceboelha?authSource=admin

# MongoDB Atlas
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/ceboelha?retryWrites=true&w=majority

# =============================================================================
# JWT - IMPORTANT: Use strong, random secrets!
# Generate with: openssl rand -hex 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# =============================================================================

# Access Token - Short-lived (15 minutes recommended for security)
JWT_ACCESS_SECRET=your-super-secret-access-key-at-least-32-characters-long
JWT_ACCESS_EXPIRES_IN=15m

# Refresh Token - Long-lived (7 days recommended)
JWT_REFRESH_SECRET=your-super-secret-refresh-key-at-least-32-characters-long
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# CORS - Comma-separated list of allowed origins
# =============================================================================
CORS_ORIGIN=http://localhost:3000

# =============================================================================
# Rate Limiting - General API
# 100 requests per minute (60000ms)
# =============================================================================
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# =============================================================================
# Rate Limiting - Authentication (stricter to prevent brute force)
# 5 attempts per 15 minutes (900000ms)
# =============================================================================
AUTH_RATE_LIMIT_MAX=5
AUTH_RATE_LIMIT_WINDOW=900000

# =============================================================================
# Security - Account Lockout (after too many failed login attempts)
# 5 attempts allowed, then locked for 15 minutes
# =============================================================================
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=900000

# =============================================================================
# Security - Password Hashing
# Higher = more secure but slower. 12 is recommended for production.
# For testing, you can use 10 for faster performance.
# =============================================================================
BCRYPT_SALT_ROUNDS=12
